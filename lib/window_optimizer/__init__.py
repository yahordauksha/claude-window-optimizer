"""Stdlib-only core logic for Claude Window Optimizer's hooks and setup/tune-up commands.

No pip dependencies, ever — this package is imported by hook scripts that
run inside whatever Python the end user's machine happens to have.
"""

__version__ = "0.1.0"
